# Documentation build requirements for Read the Docs.
# Keep in sync with the "docs" extra in pyproject.toml; the only intentional
# difference is `pandoc` below, which is needed by the RTD build environment.
sphinx>=5.0.0
sphinx-rtd-theme>=1.2.0
myst-parser>=1.0.0
nbsphinx>=0.9.0
pandoc>=2.3

# Needed only to re-execute the documentation notebooks, not to build the HTML
# (conf.py sets nbsphinx_execute = "never"). xlrd reads the UCI CTG .xls used
# by the Cardiotocography case study.
ipykernel>=6.0.0
xlrd>=2.0.1
